# Local development — keep this pointing to localhost
VITE_API_BASE_URL=http://localhost:3000/api

# Production — the CI pipeline will override this with the real Azure VM IP
# VITE_API_BASE_URL=http://<AZURE_VM_IP>:30080/api